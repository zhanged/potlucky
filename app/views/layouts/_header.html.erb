<!-- <header class="navbar navbar-default">
	<div class="navbar-inner">
		<div class="container">
			<%= link_to "Bloon", root_path, id: "logo" %>
				<nav>
				<ul class="nav pull-right">
					<% if signed_in? %>
						<li id="fat-menu" class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown"><div id="circle-nav"><%= current_user.name.at(0.1).upcase %></div><b class="caret"></b>
							</a>
							<ul class="dropdown-menu">
								<li><%= link_to "Settings", edit_user_path(current_user) %></li>
								<li>
									<%= link_to "Sign out", signout_path, method: "delete" %>
								</li>
							</ul>
						</li>
					<% else %>
							<li><%= link_to "Sign in", signin_path %></li>
					<% end %>
				</ul>
			</nav>
		</div>
	</div>
</header> -->

<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Bloon", root_path, id: "logo" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav navbar-right">
      <% if signed_in? %>
        <% if current_user.phone.present? %>
          <li><%= link_to "My List", user_path(current_user) %></li>
          <!-- <li><%= link_to "Feed", root_path %></li> -->
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name.split(' ').first %> <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <!-- <li><%= link_to "FAQ", faq_path %></li> -->
              <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
              <!-- <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li> -->
            </ul>
          </li>
        <% end %>
      <% else %>
        <li><a href="/signin" id="ga-sign-in" data-no-turbolink>Sign in</a></li>
        <!-- <li><%= link_to "FAQ", faq_path %></li> -->
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>