<% provide(:title, "Phone number") %>
<div class="form-300 all-wide-form">
	<div class="page-title">Phone Number</div>
	<div class="bullets">
		<p>So Bloon can:</p>
		<ul>
			<li>Notify you when friends invite you</li>
			<li>Keep you updated on your activities</li>
			<li>When an activity takes off, put you in a group text chat with your friends who are also joining</li>			
		</ul>
		<i>We will never send you unncessary texts</i>
	</div>
  	<%= form_for(current_user) do |f| %>
		<%= render 'shared/error_messages', object: f.object %>
		<div class="signup-phone"><%= f.text_field :phone, class: "user_phone", placeholder: "Cell Number", :autofocus => true %></div>
		<%= f.submit "Next", data: { disable_with: '...' }, class: "btn btn-large btn-block signup-phone-btn" %>
	<% end %>
    <p class="forgot-password">Standard text messaging rates apply</p>
</div>