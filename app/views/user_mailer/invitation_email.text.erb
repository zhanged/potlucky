Join <%= @gather.user.name %> for <%= @gather.activity %>?
===============================================
 
<%= @gather.tilt %> friends must join for this to happen - to join, just text Y<%= @invitation.id %> to <%= ENV['TWILIO_MAIN'] %> now.
 
<%= link_to "Join", @url %> <%= @gather.user.name %> on Potlucky and start having more fun with friends!

Note: Your cell number will receive text messages to notify you of invitations and to organize activities with friends.