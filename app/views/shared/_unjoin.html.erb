<%= form_for(Invitation.find_by(invitee: current_user, gathering: feed_item)) do |f| %>
	<%= f.hidden_field :invitation_id, value: feed_item.id %>
	<%= f.submit "Joined", class: "btn btn-large btn-block btn-joining", data: { confirm: "Are you sure you want to leave?" } %>
<% end %>